#include "comp_geom_tests.h"

// most of the code is in templates
// testing all the functions ensures that the templates are instantiated and compiled
int main(int argc, char* argv[]) {
    int result = testCompGeom();

    return result;
}